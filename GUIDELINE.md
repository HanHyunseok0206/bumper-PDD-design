# 프로젝트 개요

종합설계(졸업연구) 프로젝트. 제목: "PDD 기반의 surrogate modeling을 이용한 범퍼 강건 설계".

- 카티아로 3D 범퍼 형상을 설계하고, 아바쿠스(Abaqus/Explicit)로 충돌 FE 해석해서 SEA(비에너지흡수율)를 얻고, 그 데이터로 서로게이트를 만들어 강건 최적 설계를 진행하는 흐름.
- SEA(비에너지흡수율=흡수에너지/질량)는 최대화가 목표, "강건"은 그 값이 불확실성(재료 물성 편차, 두께 공차, 충돌 조건 등) 아래에서도 흔들리지 않게 하는 것 — 서로 다른 두 축을 합쳐 목적함수는 `maximize mean(SEA) - κ·std(SEA)` 형태로 둠. 다목적(PCF·질량 등 제약 포함 여부)은 최종적으로 지도교수/선행연구 formulation 확인 필요.
- **PDD의 역할(지도교수 지침, 2026-08-27)**: 범퍼 충돌 응답은 좌굴모드 전환 등으로 비선형·불연속적이라 PDD 같은 전역 다항식 서로게이트는 예측 정확도에 한계가 있을 수 있음. 그래서 PDD는 **민감도 분석(변수 스크리닝)** 용도로 쓰고, 실제 최적화에 쓰는 **서로게이트는 크리깅(Kriging)**으로 구축. 논문 제목("PDD 기반의 surrogate modeling")은 한 번 정하면 못 바꾸므로, "PDD 기반 민감도 분석 → 변수 선별 → 크리깅 서로게이트 구축"이라는 전체 파이프라인 자체가 "PDD 기반"이라는 논리로 방어(옵션 A). 시간 여유되면 PDD 예측을 크리깅의 트렌드 함수로 넣고 크리깅이 잔차만 학습하는 하이브리드(옵션 B, PDD가 최종 서로게이트에 문자 그대로 포함됨)로 업그레이드 — A의 결과물(DOE 데이터, PDD 계수, 1차 크리깅)이 B의 재료가 되므로 순서상 자연스러운 확장.
- 저장소: https://github.com/HanHyunseok0206/bumper-PDD-design

# 폴더 구성

- `PDD_Legendre_ver4.ipynb`: 팀원과 같이 작업 중인 PDD 서로게이트 구현체(르장드르 기저). 자세한 함수별 설명은 `README.md` 참고.
- `(2일차) ...` 폴더 및 `.zip`: 하계강습회에서 받은 배포 자료. **git에 커밋하지 않음** — 재배포 권한이 불확실해서 `.gitignore`로 제외돼 있음. 방법론 참고용으로만 쓰고, 코드를 그대로 복사해서 커밋하지 말 것.

# 이 저장소에서 작업할 때

- 커밋 메시지는 평범한 작업 로그 톤으로 작성. AI 관련 언급이나 이모지 넣지 않음.
- 커밋 author는 한현석 <han010206@hanyang.ac.kr>.
- 공동 작업 중인 코드(`PDD_Legendre_ver4.ipynb` 등)를 수정할 때는, 바뀐 부분 근처에 "무엇을/왜 바꿨는지" 짧은 주석을 남긴다.나중에 diff만 보고도 이유를 알 수 있게 하기 위함. 과하게 모든 줄에 달지 말고 실제로 바뀐 지점에만.
- 새 서로게이트/최적화 코드를 짤 때 학습 데이터만으로 R²를 재서 모델 차수를 고르지 않는다 — 반드시 별도 검증(홀드아웃 또는 교차검증) 기준으로 판단할 것. (이전에 학습셋 R²=1.0을 목표로 차수를 올리다 과적합 + 커널 크래시가 난 적 있음.)
- 실제 설계변수 범위(LB/UB)를 알고 있으면 표본 min/max가 아니라 이론적 정의역으로 스케일링할 것.
- 아래 "진행 상황" 체크리스트 항목을 (합성 데이터가 아닌 실제 결과로) 완료 처리할 때는, `PAPER.md`의 해당 절도 채울지 먼저 물어볼 것. 자잘한 코드 수정까지는 반영 안 함 — 굵직한 이정표 단위로만.

# 연구 진행 방향 (카티아 + 아바쿠스 파이프라인, 2026-08-27 갱신)

0. **설계변수 정의**: 카티아에서 바꿀 파라미터(두께, 리브 위치, 곡률 등)와 LB/UB 확정, 외부에서 구동 가능하게 설정(Design Table 또는 매크로)
1. **DOE 샘플링**: LHS로 설계공간에서 N개 점 추출 (변수 수의 8~10배 정도로 시작). FE가 병목이니 한 번에 다 뽑지 말고 예를 들어 50개로 먼저 시작 → 부족하면 추가(11단계 순차 보강과 같은 원리를 DOE 단계에도 적용)
2. **형상 자동 생성(카티아)**: DOE 점마다 매크로로 형상 생성 → FE용으로 export. 점이 많아지므로 자동화 필수
3. **FE 해석(아바쿠스)**: Abaqus/Explicit 충돌 해석. Abaqus Python 스크립팅으로 job 생성·제출·모니터링 자동화. **계산 비용 관리**: (a) 여러 job을 동시에 제출할 수 있는지(랩 서버/클러스터, 라이선스 개수) 확인 — 병렬화가 전체 일정에 제일 큰 영향을 줌. (b) 민감도 스크리닝(6단계)용 초기 DOE는 거친 메시(빠른 해석)로 돌리고, 크리깅 최종 검증/최적점 확인용 소수 점만 정밀 메시로 재해석하는 저비용-고비용 혼합 전략 고려. 이건 워크숍 자료의 다중충실도(Multi-Fidelity, `Lab3_MFMC`/`Lab4_Plate_MF`) 방법론과 같은 발상이지만, 지금은 그 정식 통계적 결합(상관계수 기반 최적 배분 등)까지는 안 쓰고 "초기엔 싸게, 나중엔 비싸게"라는 실무적 요령 수준으로만 적용 — 시간 남으면 MFMC처럼 저충실도·고충실도 데이터를 하나의 서로게이트로 정식 결합하는 것도 고려 가능.

  **참고(개념 구분)**: 다중충실도(메시 거칠기 등 시뮬레이션 자체의 정밀도 축)와 서로게이트 모델링(PDD/크리깅처럼 시뮬레이션 결과를 통계적으로 근사하는 축)은 둘 다 "비싼 것 대신 싼 것을 쓴다"는 발상은 같지만 서로 다른 축이라 "다중충실도 안에 다중충실도"가 아니라 **독립적으로 조합 가능한 두 개의 절약 수단**임. (지금 파이프라인 = 단일 충실도 FE + 서로게이트만 사용. 위에서 말한 혼합 메시 전략까지 더하면 두 축을 함께 쓰는 것.)
4. **결과 추출**: `.odb`에서 odbAccess로 흡수에너지·반력(PCF)·질량 추출, `SEA = 흡수에너지/질량` 계산
5. **데이터셋 조립**: `X`(dim × N) = 설계변수, `Y`(N,) = SEA → `PDD_bumper_SEA.ipynb`의 데이터 로딩 자리에 투입
6. **PDD 민감도 스크리닝**: `find_optimal_degree` → PDD 적합 → `get_sobol2`로 변수별 기여도 확인, 기여도 낮은 변수는 명목값 고정, `DESIGN_IDX`/`NOISE_IDX` 결정
7. **크리깅 서로게이트 구축**: 같은 DOE 데이터(또는 스크리닝 후 축소된 변수)로 크리깅 학습, 홀드아웃/교차검증 R²로 정확도 확인(학습 R²만 믿지 않기)
8. **(옵션 B, 스트레치) PDD-트렌드 하이브리드**: `residual = SEA_true - PDD예측` → 그 residual에 크리깅 적합, 최종예측 = `PDD(x) + Kriging_residual(x)`. 순수 크리깅(7) vs 하이브리드(8) 정확도 비교
9. **강건 목적함수**: 최종 서로게이트로 `estimate_robust_sea(d)` — 설계값 고정, `NOISE_IDX` 변수만 흔들어 mean/std 계산. `J(x) = mean(SEA) - κ·std(SEA)`, κ는 0~3 스윕
10. **최적화**: `scipy.optimize`로 `DESIGN_IDX`에 대해 `J(x)` 최대화, 질량/PCF 등 제약 반영
11. **검증**: 최적점을 카티아로 재형상화 → 아바쿠스 재해석 → 서로게이트 예측과 비교, 오차 크면 DOE에 추가 후 6~7단계 재적합(순차 보강)
12. **정리**: 민감도 분석 결과, PDD vs 크리깅 vs 하이브리드 정확도 비교, κ별 강건-결정론적 최적 설계 트레이드오프가 논문 핵심 결과

2~3번(카티아/아바쿠스 자동화)이 준비 안 되면 가장 시간이 많이 드는 구간이라 우선순위 높게 챙길 것.

## AI 요소 추가 (스트레치, 지도교수 코멘트 반영)

지도교수님이 미팅에서 "AI를 섞어도 좋을 것 같다"고 언급함. 지금 파이프라인은 크리깅(GP)이 이미 머신러닝이긴 하지만 통계적 회귀모델처럼 보여서, 아래 두 개를 7·10·11단계에 추가로 얹어 "AI를 도입했다"는 서사를 명확히 하는 것을 고려.

핵심 아이디어는 하나: 크리깅이 예측값과 함께 주는 **불확실성(예측 표준편차)**을 어디에 쓰느냐로 두 갈래로 나뉨.

| 기법 | 쓰이는 곳 | 난이도 | 혁신성 | 완주 가능성 | 비고 |
|---|---|---|---|---|---|
| **능동학습(Active Learning)** | 7, 11단계 — 서로게이트 자체를 더 정확하게 | ★☆☆☆☆ | ★★☆☆☆ | ★★★★★ | 크리깅이 제일 불확실해하는 지점에 다음 FE를 추가. 기존 코드에 몇 줄 추가 수준 |
| **베이지안 최적화(Bayesian Optimization)** | 10단계 — 강건 최적점 탐색 | ★★☆☆☆ | ★★★★☆ | ★★★★☆ | 예측값+불확실성 기반 획득함수로 다음 후보 선택. 능동학습과 같은 엔진 재사용 가능. 하이퍼파라미터 튜닝/AutoML에서 쓰는 바로 그 기법이라 "AI" 라벨이 명확함 |
| (참고, 우선순위 낮음) SAEA | 10단계 대체 | ★★★☆☆ | ★★★☆☆ | ★★★☆☆ | 강건 목적함수가 다봉형(multimodal)으로 확인될 때만 고려 |
| (참고, 우선순위 낮음) 다중충실도 BO | 3, 10단계 | ★★★★★ | ★★★★★ | ★★☆☆☆ | 제일 혁신적이지만 이미 계획된 작업량 위에 얹기엔 리스크 큼 |

**추천 순서**: 능동학습 → 베이지안 최적화. 둘 다 크리깅의 불확실성을 재사용하는 거라 구현이 상당 부분 겹침. 7단계(크리깅 구축) 실데이터로 끝난 뒤, 시간 여유 봐서 진행.

## (참고, 현재는 미사용) PDD 자체를 최종 서로게이트로 쓸 경우의 최적화 방법 비교

지도교수 지침에 따라 PDD는 이제 6번(민감도 분석) 용도로만 쓰고, 실제 서로게이트/최적화는 크리깅으로 진행. 아래 표는 "PDD를 최종 서로게이트로 계속 썼다면"의 조사 결과라 지금 파이프라인엔 직접 적용되지 않지만, 크리깅 기반 강건 목적함수를 설계할 때 개념(그래디언트 vs 블랙박스 탐색, 순차 보강 등)은 여전히 참고할 만해서 남겨둠.

| 방법 | 푸는 문제 | 기저 절단 방식 | 민감도(그래디언트) | 필요 해석 수 | 장점 | 단점 | 적합도 |
|---|---|---|---|---|---|---|---|
| 표준 PDD-RDO | 강건설계 (mean±κ·std) | S, m을 사람이 직접 지정 | PDD 계수 직접 미분 | 적음(수백 점) | 구현 쉬움, 지금 코드 그대로 확장 | 변수 많아지면 항 수 조절을 감으로 해야 함 | ★★★★★ 지금 바로 적용 |
| AS-PDD (Adaptive-Sparse) | 강건설계 (고차원) | 전역민감도 기준 자동 선택 | Score function 기반 해석적 미분 | 표준보다 적음 | 절단 파라미터 사전지정 불필요, 고차원(수십 변수)에 강함 | 알고리즘 구현 복잡(희소회귀 등) | ★★★☆☆ 아이디어만 간이 차용 |
| AS-PDD-SPA (RBDO) | 신뢰성 기반 (P(파괴)≤목표) | AS-PDD + 새들포인트근사 | Score function | 중간 | 확률 제약을 정확히 다룸 | 구현 난이도 높음, 이론 배경 필요 | ★★☆☆☆ 하드 안전제약 있을 때만 |
| AS-PDD-MCS (RBDO) | 신뢰성 기반 | AS-PDD + 서로게이트 위 내장 MCS | Score function | 중간~많음 | SPA보다 직관적, 비선형성에 안정적 | MCS라 SPA보다 느림 | ★★☆☆☆ 위와 동일 |
| PDD + GA/PSO (미분 없이) | 강건설계/다목적 | 표준 또는 adaptive | 없음(블랙박스 탐색) | 적음(서로게이트가 싸서 반복은 많아도 됨) | 다목적(SEA vs PCF vs 질량) Pareto 탐색에 유리 | PDD의 해석적 미분 장점을 못 씀 | ★★★★☆ 다목적 확장 시 유용 |
| 순차/적응 보강형 PDD | 위 어떤 문제든 | 표준(반복적으로 보강) | 표준과 동일 | 초기엔 적고 필요시 추가 | 서로게이트 오차를 최적화 후에 보정, 구현 부담 적음 | 반복 루프 관리 필요 | ★★★★★ 5단계 보완용으로 추천 |
| BNN-PDD (최신 하이브리드) | 비선형 강건설계 | 국소 서브영역별 PDD + BNN | BNN 예측 기반 | 많음(액티브러닝 필요) | 강한 비선형 응답에 정확도 높음 | 구현·튜닝 부담 큼, 최신 연구라 자료 적음 | ★☆☆☆☆ 현재 스코프엔 과함 |

(2026-08-27 기준 위 표는 참고용 — 실제 서로게이트는 크리깅으로 진행하고, 위 개념 중 "순차 보강"은 파이프라인 11단계에 그대로 반영됨)

# 진행 상황 (최근 업데이트: 2026-08-27)

- [x] PDD_Legendre_ver4.ipynb 1차 검증 및 수정 (학습/검증 분리 기반 차수 선택, 스케일링 수정)
- [x] PDD 최적화 방법론 조사 (참고용 표로 남김, 실제 적용은 아래로 대체됨)
- [x] 지도교수 지침 반영: PDD=민감도 분석, 크리깅=서로게이트/최적화로 역할 확정, 카티아+아바쿠스 기준 12단계 파이프라인 정리
- [x] `PDD_bumper_SEA.ipynb`를 PDD 민감도 분석 전용으로 범위 축소, 나머지는 참고용으로 표시
- [x] `Kriging_bumper_SEA.ipynb` 골격 작성 (LHS DOE 생성, 크리깅 적합+검증, 강건 목적함수, 최적화)
- [x] `PDD_vs_Kriging_benchmark.ipynb`로 합성 벤치마크(변수 상호작용 있는 꺾인 함수) 비교 — 크리깅 R²=0.965 vs PDD R²=0.930으로 지도교수 지침을 실증. (주의: 벤치마크 함수가 순수 additive/커널 nu 고정이면 반대로 PDD가 이길 수도 있음 확인함 — 실제 데이터에서도 반드시 홀드아웃 검증으로 재확인할 것)
- [ ] 카티아 설계변수 자동 형상 생성 매크로 준비
- [ ] 아바쿠스 FE 해석 자동화(job 생성·제출) 및 결과(SEA/PCF/질량) 추출 스크립트 준비
- [ ] DOE 샘플링 + 실제 FE 해석으로 (X, Y) 데이터셋 확보
- [ ] `PDD_bumper_SEA.ipynb`로 PDD 민감도 스크리닝, DESIGN_IDX/NOISE_IDX 결정
- [ ] `Kriging_bumper_SEA.ipynb`에 실데이터 적용, 홀드아웃 검증 (필요시 PDD-트렌드 하이브리드 비교)
- [ ] 강건 목적함수(mean-κ·std) 구성 및 최적화
- [ ] 최적점 카티아/아바쿠스 재해석으로 서로게이트 검증 및 필요시 순차 보강
- [ ] (스트레치, AI 요소) 능동학습으로 서로게이트 보강 → 베이지안 최적화로 강건 최적점 탐색
