# 프로젝트 개요

종합설계(졸업연구) 프로젝트. 제목: "PDD 기반의 surrogate modeling을 이용한 범퍼 강건 설계".

- 카티아로 3D 범퍼 형상을 설계하고, PDD(Polynomial Dimensional Decomposition) 서로게이트로 SEA(비에너지흡수율)를 예측해 강건 최적 설계를 진행하는 흐름.
- SEA(비에너지흡수율=흡수에너지/질량)는 최대화가 목표, "강건"은 그 값이 불확실성(재료 물성 편차, 두께 공차, 충돌 조건 등) 아래에서도 흔들리지 않게 하는 것 — 서로 다른 두 축을 합쳐 목적함수는 `maximize mean(SEA) - κ·std(SEA)` 형태로 둠. 다목적(PCF·질량 등 제약 포함 여부)은 최종적으로 지도교수/선행연구 formulation 확인 필요.
- 저장소: https://github.com/HanHyunseok0206/bumper-PDD-design

# 폴더 구성

- `PDD_Legendre_ver4.ipynb`: 팀원과 같이 작업 중인 PDD 서로게이트 구현체(르장드르 기저). 자세한 함수별 설명은 `README.md` 참고.
- `(2일차) ...` 폴더 및 `.zip`: 하계강습회에서 받은 배포 자료. **git에 커밋하지 않음** — 재배포 권한이 불확실해서 `.gitignore`로 제외돼 있음. 방법론 참고용으로만 쓰고, 코드를 그대로 복사해서 커밋하지 말 것.

# 이 저장소에서 작업할 때

- 커밋 메시지는 평범한 작업 로그 톤으로 작성. AI 관련 언급이나 이모지 넣지 않음.
- 커밋 author는 한현석 <han010206@hanyang.ac.kr>.
- 공동 작업 중인 코드(`PDD_Legendre_ver4.ipynb` 등)를 수정할 때는, 바뀐 부분 근처에 "무엇을/왜 바꿨는지" 짧은 주석을 남긴다.나중에 diff만 보고도 이유를 알 수 있게 하기 위함. 과하게 모든 줄에 달지 말고 실제로 바뀐 지점에만.
- 새 서로게이트/최적화 코드를 짤 때 학습 데이터만으로 R²를 재서 모델 차수를 고르지 않는다 — 반드시 별도 검증(홀드아웃 또는 교차검증) 기준으로 판단할 것. (이전에 학습셋 R²=1.0을 목표로 차수를 올리다 과적합 + 커널 크래시가 난 적 있음.)
- 실제 설계변수 범위(LB/UB)를 알고 있으면 표본 min/max가 아니라 이론적 정의역으로 스케일링할 것.

# 연구 진행 방향 (5단계)

1. **서로게이트 구축**: 카티아 설계변수 → DOE 샘플링 → 각 점 FE 크래시 해석 → SEA 산출 → `PDD_Legendre_ver4.ipynb`의 표준 PDD(S≤2~3, 저차 다항식)로 근사, 검증셋 R²로 정확도 확인
2. **변수 스크리닝**: `get_sobol2`로 변수/상호작용별 SEA 기여도 확인, 기여도 낮은 변수는 명목값으로 고정해 최적화 대상 변수 수를 줄임
3. **강건 목적함수 구성**: `J(x) = mean_PDD(SEA) - κ·std_PDD(SEA)`. PDD 계수만으로 평균·분산 계산(추가 샘플링 불필요). κ는 0~3 정도로 스윕하며 트레이드오프 확인 (강습회 `Task2_RDO` 패턴)
4. **최적화**: PDD 서로게이트가 싸므로 `fmincon`류 gradient-based 옵티마이저(강습회 `Task2_RDO` 구조) 또는 GA/PSO 등 전역탐색 사용. 필요시 질량 상한, 최대 침입량/PCF 등 제약 추가
5. **검증**: 최적 설계점을 실제 카티아+FE로 재해석해 서로게이트 예측과 비교, 오차가 크면 그 근처에 샘플 추가 후 재적합(순차 보강)

한 학기 스코프에서는 이 5단계가 현실적인 완주 범위. AS-PDD의 자동 절단이나 RBDO의 파괴확률 계산 같은 고급 기법은 2단계(민감도 스크리닝)+5단계(순차 보강)로 상당 부분 효과를 대체하고, 필요할 때만 추가 검토.

## PDD 최적화 방법 비교

| 방법 | 푸는 문제 | 기저 절단 방식 | 민감도(그래디언트) | 필요 해석 수 | 장점 | 단점 | 적합도 |
|---|---|---|---|---|---|---|---|
| 표준 PDD-RDO | 강건설계 (mean±κ·std) | S, m을 사람이 직접 지정 | PDD 계수 직접 미분 | 적음(수백 점) | 구현 쉬움, 지금 코드 그대로 확장 | 변수 많아지면 항 수 조절을 감으로 해야 함 | ★★★★★ 지금 바로 적용 |
| AS-PDD (Adaptive-Sparse) | 강건설계 (고차원) | 전역민감도 기준 자동 선택 | Score function 기반 해석적 미분 | 표준보다 적음 | 절단 파라미터 사전지정 불필요, 고차원(수십 변수)에 강함 | 알고리즘 구현 복잡(희소회귀 등) | ★★★☆☆ 아이디어만 간이 차용 |
| AS-PDD-SPA (RBDO) | 신뢰성 기반 (P(파괴)≤목표) | AS-PDD + 새들포인트근사 | Score function | 중간 | 확률 제약을 정확히 다룸 | 구현 난이도 높음, 이론 배경 필요 | ★★☆☆☆ 하드 안전제약 있을 때만 |
| AS-PDD-MCS (RBDO) | 신뢰성 기반 | AS-PDD + 서로게이트 위 내장 MCS | Score function | 중간~많음 | SPA보다 직관적, 비선형성에 안정적 | MCS라 SPA보다 느림 | ★★☆☆☆ 위와 동일 |
| PDD + GA/PSO (미분 없이) | 강건설계/다목적 | 표준 또는 adaptive | 없음(블랙박스 탐색) | 적음(서로게이트가 싸서 반복은 많아도 됨) | 다목적(SEA vs PCF vs 질량) Pareto 탐색에 유리 | PDD의 해석적 미분 장점을 못 씀 | ★★★★☆ 다목적 확장 시 유용 |
| 순차/적응 보강형 PDD | 위 어떤 문제든 | 표준(반복적으로 보강) | 표준과 동일 | 초기엔 적고 필요시 추가 | 서로게이트 오차를 최적화 후에 보정, 구현 부담 적음 | 반복 루프 관리 필요 | ★★★★★ 5단계 보완용으로 추천 |
| BNN-PDD (최신 하이브리드) | 비선형 강건설계 | 국소 서브영역별 PDD + BNN | BNN 예측 기반 | 많음(액티브러닝 필요) | 강한 비선형 응답에 정확도 높음 | 구현·튜닝 부담 큼, 최신 연구라 자료 적음 | ★☆☆☆☆ 현재 스코프엔 과함 |

결론: 표준 PDD-RDO로 시작 → Sobol 스크리닝으로 AS-PDD 효과를 흉내 → 마지막에 순차 보강 한 번. 이 조합이 한 학기·카티아+FE 비용 제약 안에서 제일 현실적.

# 진행 상황 (최근 업데이트: 2026-08-25)

- [x] PDD_Legendre_ver4.ipynb 1차 검증 및 수정 (학습/검증 분리 기반 차수 선택, 스케일링 수정)
- [x] PDD 최적화 방법론 조사 및 연구 방향 확정 (위 5단계 + 방법 비교)
- [ ] 카티아 범퍼 설계변수 + FE 해석(SEA) 결과로 실데이터 적용
- [ ] Sobol 기반 변수 스크리닝으로 최적화 대상 변수 축소
- [ ] PDD 계수 기반 mean/variance로 강건 최적화(RDO) 목적함수 구성 및 fmincon 최적화
- [ ] 최적점 카티아/FE 재해석으로 서로게이트 검증 및 필요시 순차 보강
